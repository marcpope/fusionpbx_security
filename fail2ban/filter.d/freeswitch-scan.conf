# Fail2Ban filter for FreeSwitch SIP scanning
# Catches registration attempts for non-existent users (any domain)

[Definition]

failregex = \[WARNING\] sofia_reg\.c:\d+ Can't find user \[.*\] from <HOST>

ignoreregex =
